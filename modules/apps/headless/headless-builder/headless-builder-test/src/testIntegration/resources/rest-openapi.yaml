components:
    schemas:
        TestEntry:
            properties:
                date:
                    description:
                        The test entry date.
                    format: date-time
                    readOnly: true
                    type: string
                    x-field-definition:
                        name: dateField
                number:
                    description:
                        The test entry number.
                    format: int64
                    type: integer
                    x-field-definition:
                        name: numberField
            x-schema-definition:
                entityName: "com.liferay.headless.builder.test.model.TestEntry"
info:
    license:
        name: "Apache 2.0"
        url: "http://www.apache.org/licenses/LICENSE-2.0.html"
    title: ""
    version: v1.0
openapi: "3.0.1"
paths:
    "/test-entries/{id}":
        get:
            operationId: getTestEntry
            parameters:
                - in: path
                  name: id
                  required: true
                  schema:
                      format: int64
                      type: integer
            responses:
                200:
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/TestEntry"
            tags: ["TestEntry"]
            x-operation-definition:
                type: "getByPrimaryKey"